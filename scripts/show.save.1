#!/bin/bash
#
# ファイルかディレクトリかを気にせずに表示できる
#
# show [ FILE | DIR ]

if [ "$#" ]; then

fi

for input in "$@"
do
  if [ -d "$input" ]; then
    ls "$input"
  elif [ -f "$input" ]; then
    less "$input"
  else
    echo "Sorry, i dont no ..."
  fi
done
